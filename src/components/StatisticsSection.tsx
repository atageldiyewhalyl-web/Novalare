import { motion } from "motion/react";
import { TrendingUp, FileText, AlertCircle, Sparkles, CheckCircle, ArrowRight } from "lucide-react";
import { useTheme } from "@/contexts/ThemeContext";

export function StatisticsSection({ companies }: { companies?: any[] }) {
  // Statistics section removed - keeping component for backward compatibility
  return null;
}